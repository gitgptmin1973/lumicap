# LUMICAP Ubuntu BuildKit

Ubuntu 22.04以降のターミナルでこのフォルダーを開き、次を実行します。

```bash
chmod +x build-ubuntu.sh
./build-ubuntu.sh
```

Node.js 22とnpmが必要です。完了後、`release` フォルダーへAppImageとdebを生成します。

Waylandでは、初回の画面取得時にOSの画面共有確認が表示される場合があります。PrintScreenが他のアプリで使用中の場合は `Ctrl+Shift+1` を使用してください。

